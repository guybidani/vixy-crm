# Gitleaks configuration — https://github.com/gitleaks/gitleaks
title = "Vixy CRM Secret Scanning"

[allowlist]
  paths = [
    '''node_modules''',
    '''\.env\.example''',
    '''package-lock\.json''',
  ]
